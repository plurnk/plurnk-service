# Changelog

Generated by `scripts/changelog.mjs` from commit subjects between release tags.
Chores, tests, builds, and tooling commits are omitted. Regenerate with
`npm run changelog -- --write`; `npm run changelog` prints what is pending.

## 1.19.2 — 2026-09-21

Fixes:

- quotation: an offset example is prose, and draws no complaint (#799)
- providers: the installed inventory and spec drop the xai adapter (#795)

## 1.19.1 — 2026-09-21

Fixes:

- release: the parser smoke speaks the four-backtick fence (#761)

Refactors:

- reasoning: no reasoning is carried that cannot be read (#795)

## 1.19.0 — 2026-09-20

Features:

- turn: a lost boundary refuses only what follows it (#781)
- turn: wait parks until the next wake; its join retires (#780)
- a2a: the exposure rides the daemon's listener; host/port retire (#641)
- http: core owns the one listener; adapters mount routes (#641)
- posture: a WebSocket frame proposes before it is written (#777)
- posture: a fresh install mints its own bearer (#774)
- posture: an outbound mutation proposes before anything leaves (#774)
- posture: reject unapproved, trust @plurnk only, stop the lie (#774)
- agui: the origin a browser may read replies from is a knob (#774)
- a2a: the family is a2a, and its scheme is the runtime's live half (#771)
- env: a key the daemon and its clients share has a shared owner (#771)
- env: retry pacing, a listing preview and llms.txt become knobs (#771)
- read: the first page ships at a hundred units, not sixteen (#771)
- gate: a number named as a tunable is a knob or registered mechanism (#771)
- policy: the panel supplies what a loop's creator leaves unsaid (#771)
- gate: an anchor is witnessed by implementation, a panel or a test (#771)
- gate: the cascading environment is the only home for a choice (#771)
- capabilities: the loop is the cascade's innermost ring (#770)
- engine: a run declares its attendance; unattended never waits (#765)
- contracts: one constant names the operation fence (plurnk/plurnk#92)
- messages: the model addresses a message by its short name
- mcp: project schemas as json and emit valid preview skeletons (#762)
- engine: a loop's answer lives at ops://worker/loop (#761)
- parser: an operation opens with four backticks; three is markdown (#761)
- engine: a response with no operations is the answer (#761)
- db: store each body once by hash; search reads the store (#764)
- db: operator knobs for auto-vacuum mode and the reclaim floor (#764)
- digest: report database storage health; read pre-claim loops (#764)
- loops: record a loop's first claim and report wake latency (#703)
- db: the daemon reclaims its own free pages (#764)
- http: an operator host policy for web acquisition (plurnk/plurnk-bench#38)
- schedule: retain loop-local waits for finite occurrences (#747)
- runtime: introduce notes and explicit lifecycle verbs (#713)
- a2a: bridge message resources and durable protocol history (#702)
- schedule: read the rule's zone through the invoking worker's env (#707)
- schedule: add the schedule family on RFC 5545 rules (#707)
- media: carry native audio through ordinary READ (#689)

Fixes:

- proposal: a harness-decided rejection says why and how out (#789)
- test: one artifact home; a database is born where it lives (#790)
- engine: a proposal wait ends with its loop (#769)
- parser: a reasoning tag is text, so a shown example stays data (#784)
- gate: an unconfigured identity register says so (#782)
- config: the committed gate profile names no model (#782)
- docs: the repository stands on its own, not on one machine (#782)
- curation: a log KILL that matched nothing says so, once (#779)
- e2e: the installation drill registers the packaged SQL functions (#776)
- namespace: drop the jail metaphor; a refusal names the real rule (#774)
- env: the plugin trust gate reads the panel that owns it (#771)
- log: an unscoped pattern KILL no longer retires the whole glob (#771)
- a2a: a glob over an agent's resources finds them again (#771)
- docs: the project's AGENTS.md keeps the standard's name at turn 0 (#768)
- mcp: the overlay contract restates no host control key (plurnk/plurnk#93)
- env: a family's enabled list is stated, never assumed empty (#771)
- file: a miss speaks of membership and never claims absence (#771)
- env: the prompt page is a knob; A2A's page size is the protocol's (#771)
- env: core declares its members family; unset never means none (#771)
- env: effect admission is one knob per effect, none held in code (#771)
- env: page sizes, the log read ceiling and a sample size are knobs (#771)
- env: eleven core choices written as numbers move to the panel (#771)
- read: one panel knob is the first page of every markerless retrieval (#771)
- a2a: an inbound loop's proposal disposition is a panel choice (#771)
- env: tests run on their own panel; code defaults and a hidden UA go (#771)
- env: six hidden provider knobs are declared, and shed means retired (#771)
- env: every in-code value and fallback reader yields to the panel (#771)
- gate: reading the system environment is the mechanism, never a debt (#771)
- capabilities: the loop ring's refusal names its reason and recovery (#770)
- digest: a turn line names its packet artifact and the model's own count
- parser: a fence that opens no operation quotes what it holds (#767)
- search: no default size bound; a large body is searchable (#764)
- providers: stop cutting off streams that are still producing (#764)
- parser: a closer followed only by an aside still closes (#758)
- exec: read a directory or label target by its one meaning (#758)
- search: skip indexing bodies larger than a byte bound (#729)
- tools: show a short required enum's values in tool previews (#762)
- read: past-end ranges read empty; command rows name their stream (#759)
- mcp: read a trailing aside after tool arguments as an aside (#758)
- edit: tolerate an offset on a hash anchor without teaching it (#749)
- parser: read unambiguous heading near-misses without diagnostics (#758)
- parser: read native tool-call markup naming a plurnk operation (#760)
- wait: skip any WAIT scope unread instead of refusing it (#756)
- exec: kill of a process that already ended succeeds with its status (#757)
- providers: read and request gemini gateway thoughts (#754)
- parser: report extra operands without guessing intent (#753)
- metadata: preserve stream READ lifecycle facts end to end (#752)
- mcp: keep managed tool state outside project trees (#751)
- read: name the recorded stream in an empty-extent 416 (#750)
- schedule: accept shared worker identifiers as targets (#748)
- compose schedule capabilities in live and demo workspaces (#746)
- http: apply shared-cache reuse policy within workspaces (#674)
- a2a: preserve and project response format preferences (#718)
- http: validate stored response identity before 304 reuse (#745)
- http: restrict reuse to cache-eligible complete responses (#744)
- http: separate origin status from producer lifecycle (#743)
- skills: align names across discovery and resource addresses (#675)
- send: distinguish replies from new messages in recovery (#739)
- observe: keep received messages out of child activity (#738)
- config: let empty definitions mask inherited functionality (#737)
- mcp: advertise purpose without losing tool menus (#733)
- agui: preserve identity for mid-loop prompts (#736)
- wait: ignore non-scheduling header decorations (#735)
- messages: identify answered messages in reply receipts (#722)
- schemes: honor the public KILL handler contract (#731)
- parser: preserve matchers beside owner metadata (#730)
- tooling: honor the configured conformance client checkout (#656)
- worker: admit case-sensitive alphanumeric identifiers (#725)
- digest: bound assertion failures and release forensic readers (#728)
- digest: preserve input evidence across output refreshes (#727)
- parser: preserve URL component boundaries under WPT replay (#668)
- observe: align GenAI spans and aggregate request usage (#673)
- parser: contain diagnostics and verify JSONPath admission (#666)
- mimetypes: preserve CSV records and their source evidence (#670)
- mimetypes: use Node dotenv semantics with source-located values (#669)
- mcp: bound complete discovery walks by the connection deadline (#703)
- core: name missing targets without implying a required scheme (#711)
- core: acknowledge completions at the packet observation boundary (#722)
- parser: prefer balanced nesting before fence recovery (#726)
- agui: validate interrupt resumes before loop settlement (#724)
- core: qualify metadata diagnostics by resource operation (#702)
- core: keep SEND metadata ownership separate from resource options (#702)
- lifecycle: finish message and schedule contract cleanup (#712)
- exec: re-check cancellation after env resolution before the run (#709)
- a2a: keep task observation from creating workspaces (#663)
- a2a: retain received media as ordinary readable resources (#702)
- a2a: verify HTTP conformance and repair request projections (#663)
- mcp: decouple Task lifetime from shared watch setup (#701)
- mcp: expire client input with its operation budget (#700)
- mcp: settle cancellation before closing connections (#699)
- mcp: acknowledge catalog changes only after publication (#698)
- mcp: expose elicitation decisions to schema clients (#601)
- core: observe live work and expose scheduled obligations (#693)
- core: preserve empty binary resources in transfers (#690)
- http: retain binary sources through native READ (#690)
- mcp: project prompt media through ordinary resources (#601)
- mcp: keep unsupported catalogs from disabling servers (#688)
- functionality: preserve managed failures and recovery paths (#687)
- mimetypes: preserve source query coordinates in live resources (#686)
- parser: accept js as an unadvertised node spelling (#685)
- env: compose workspace defaults into MCP launches (#684)

Refactors:

- mimetypes: the framework counts no tokens (#791)
- exec: one lifetime field; the cadence is the daemon's (#708)
- providers: the first-party plurnk route and its keys are gone (#771)
- env: the last code-side defaults go; the allowance is empty (#771)
- mcp: one retry pacing, and default stays a panel word (#771)
- engine: one address for a loop; loop:// retires into ops:// (#766)
- packet: unify extent metadata with scope notation (#752)
- metadata: unify packet operand addresses (#752)
- observe: separate messages from durable loop outcomes (#741)
- observe child actions and qualify history sources (#734)
- digest: remove unused request-ledger join (#678)
- contracts: remove unreferenced schema definitions (#665)
- core: conclude answered work through durable messages (#721)
- core: drop TASK wait timing; a wait joins live work only (#707)
- core: drop SEND scheduling and recurrence for schedule rules (#707)
- core: dissolve prompt:// into inbound messages (#706)
- core: point active prompts at their causal sources (#706)
- agui: name the run's message as the prompt's causal source (#706)

Documentation:

- contracts: a prose turn may contain GFM (plurnk/plurnk#96)
- contracts: the wait line says what it does, not how (#780)
- contracts: the headline sells the slipstream (#778)
- agents: a retired client is not the field guide's opening line (#768)
- spec: the last anchors find a witness; the allowance is empty (#771)
- spec: 179 anchors gain an honest witness, a home, or a correction (#771)
- mcp: the teaching opens with the tool call, not the transport (#768)
- architecture: state configuration authority where it is read first (#771)
- agents: the dogfood teamwork session is a remembered drill
- worker: example addresses name example.md, never a plausible file
- skills: expose executable copy and move examples (#722)
- packet: align remaining preview references (#752)
- language: use path placeholders for copy and move (#753)
- mcp: use a generic server configuration example (#0)
- readme: remove private forge references (#0)
- language: show each copy and move operand scope (#722)
- clarify NOTE also persists from reasoning (#722)
- align output address examples with claimed identities (#484)
- mimetypes: distinguish naming evidence from registration (#672)
- send: remove retired entry-deletion coverage claims (#739)
- config: describe workspace-owned functionality (#737)
- derive gate selection guidance from its owner (#656)
- use a neutral anchored-regex example (#722)
- contract: surface NOTE support in reasoning (#722)
- test: remove retired model aliases from fixture guidance (#484)
- align initialization survey terminology (#722)
- clarify replies and initialization evidence (#722)
- demonstrate program and reasoning notes in turn0 (#720)
- public: the first screen shows one command and a recorded session (#696)
- http: align channel tables and lifecycle with source retention (#690)
- release: record platform 1.18.0 changelog (#682)

## 1.18.0 — 2026-09-15

Features:

- dispatch: fanned-out READ rows name their glob (plurnk/plurnk#67)
- config: sqlite readers knob sizes the database pool; ships zero (#655)
- retention: a call's response body is its own row and may retire (#614)
- members: the model may add members by default (#646)
- language: a task not yet started is todo, never pending (#619)
- env: the heading's env option on executor fences, WORK and FORK (#586)
- env: each spawn records its environment; the digest renders it (#586)
- env: a child inherits its parent's entries, named for their source (#586)
- env: a worker's own environment reaches its spawns (#586)
- env: register the family and act for the invoking worker (#586)
- functionality: a family declares its alias grammar (#586)
- env: the environment family adapter and its contract (#586)
- env: the catalog projects addable candidates with provenance (#586)
- functionality: a family declares who owns its definitions (#586)
- functionality: module state can be owned by a worker (#586)
- env: the configuration catalog discover projects (#586)
- env: a WORK child inherits a copy of its parent registry (#586)
- env: a worker registry composes over the ambient ceiling (#586)
- env: the executor framework owns the subprocess floors (#586)
- env: the ambient EXEC policy is a ceiling over the invariant (#586)
- language: a closer mid-heading reads as if it were not written (#643)

Fixes:

- parser: qualify the unpublished candidate dependency pair (#682)
- core: offer quiet completion after an unchanged final response (#682)
- agui: share TASK projection across live and history rows (#681)
- agui: serialize first conversation binding (#661)
- agui: op.look resolves as the conversation worker (plurnk/plurnk#68)
- daemon: model controls and thread names bind model workers only (#658)
- core: turn outcomes carry the written op (#659)
- core: executions call exec; a pending proposal keeps its runtime (#659)
- core: type-only runtime tags; internal row ops are not executions (#659)
- agui: the announcing row opens an execution's stream (plurnk/plurnk#67)
- service: an open failure on an older baseline names the remedy (#614)
- lifecycle: a completion over live work joins it, never a strike (#619)
- functionality: runtimes are the resident family's facet, not a phase (#647)
- lifecycle: settled results defer a terminal; only live work refuses (#619)
- gate: the worktree drill builds the pushed sources first (#642)
- gate: the drill runs the pushed commit in a throwaway worktree (#642)
- packet: the withholding receipt states its past condition (#645)
- lifecycle: the loop's response is its last delivered message (#619)
- env: discover lists only settable names, matched by name or comment (#586)
- gate: refuse a dirty tree and scope intg to the pushed sha (#642)
- contracts: a closing fence with trailing text says so (#643)

Performance:

- search: judge channels by stored hash; acquire only derived bodies (#649)
- catalog: line counts are stored; the listing reads no bodies (#614)

Refactors:

- contracts: retire reserved worker names; plurnk mints freely (#660)
- core: the runtime actor is named _plurnk and found by origin (#660)
- core: executions dispatch by runtime tag; the EXEC name is gone (#659)
- contracts: an execution is its runtime fence, never an EXEC op (#659)
- types: narrow instead of assert where the guard exists (#638)
- contracts: the parser moves to @plurnk/plurnk-parser (#653)
- core: one turn runs as six phases over typed records (#630)
- packet: a log row renders through four stages (#635)
- schemes: the data statement runner binds once and runs per op (#637)

Documentation:

- contracts: clarify log curation wording (#682)
- spec: executions are runtime fences; EXEC leaves the vocabulary (#659)
- public: objective front matter; generated changelog gated by tags (#643)
- spec: claim answers never strike; malformed TASK is a hard result (#657)
- spec: remove obsolete disposition-strike restatement (#657)
- spec: consolidate core ownership and domain navigation (#657)
- spec: remove obsolete transfer and matcher descriptions (#657)
- teaching: graph patterns name symbols; bare patterns are extglob (#484)
- teaching: scope example spans lines; all dialects, all mimetypes (#484)
- teaching: the WORK example's env option is terse (#586)
- teaching: the env option rides the WORK and sh examples (#586)
- teaching: the SEND line names the final response (#0)
- schemes: the http and wss references illustrate SEND as a block (#643)
- public: prose names an operation, never an inline fence (#643)

Reverts:

- env: the registry is family state, not a workspace entry (#586)

## 1.17.0 — 2026-09-13

Features:

- contracts: an executor tag matches its registered name in any case (#615)
- streams: terminal on stream reads; one conclusion row; rev ditched (#615)
- language: a whole turn on one line parses (#615)
- language: slots after a bare matcher peel off the right end (#615)
- language: a leading (?i) lifts into the regex flags (#615)
- reasoning: turn-0 note line and the ^Note: pluck (#615)
- language: a glob READ reads every matching path, never a FIND (#615)
- language: glob pattern READ fans out; <@hash 42> is the anchor (#615)
- language: naked patterns on the heading; turn 0 plucks NOTE: lines (#615)
- language: indented fence lines are fence lines; teaching edits (#615)
- language: five tolerances, empty turns admitted with a strike (#615)
- language: anchored fences, numeric delimiters, no counting (#615)
- language: admit TASK anywhere in a turn (#615)
- db: retention is the operator's policy under retention-policy (#614)
- read: a receipt names the resource's other channels with tokens (#622)
- http: the tavily materializer plugin ships with the service (#622)
- catalog: a tool family summary names every tool, shown whole (#577)
- packet: one delegation section lists live workers and streams (#621)
- language: heading pattern selects lines on read/edit/kill/copy/move (#591)
- history: turn line on the packet; empty reasoning over absent (#613)
- language: a non-worker path on WORK or FORK is the prompt (#616)
- packet: the Worker block carries the date and time zone (#618)
- send: accept a SEND to this loop's own prompt as the response (#620)
- schemes: metadata is one JSON array of option objects (#591)
- exec: deliver live input through invocation-owned receivers (#600)
- mcp: compose typed resources with native READ delivery (#601)
- contracts: the disposition send ends the turn (#0)
- mimetypes: f# is an optional grammar leaf, not a default (#541)
- mimetypes: a pdf is a native attachment, never extracted (#542)
- engine: a log-region move is a copy plus the log's own kill (#515)
- agui: count the bound worker's alive children on the status gauge (#523)
- parser: advise when a body swallows headings of another lane (#515)
- worker: expose minted kind and lifecycle on worker observation (#523)
- worker: record whether the reasoning policy was chosen or seeded (#528)
- runtime: restore BARE resource prompts and lifecycle teaching (#575)
- runtime: schedule directed SEND through durable worker tasks (#573)
- models: expose admitted reasoning policies in catalog (#529)
- reasoning: expose editable history with immutable originals (#539)

Fixes:

- release: installation smokes speak the current language (#615)
- providers: rate witnesses derive from the catalog, never a literal (#615)
- core: the plan-slotless witness reads the block boundary diagnostic (#615)
- contracts: the fragment witness narrows its op; regenerated types (#615)
- language: a bare path takes #channel like a URL (#615)
- contracts: the glob READ witness types its op as READ (#615)
- packet: scheduled tasks are due in minutes, no clock (#618)
- packet: the worker block follows the log with loop/turn, no date (#618)
- language: a body on FIND, READ or KILL is an advisory (#615)
- rails: an empty TASK is a soft receipt, never a strike (#615)
- core: refuse a reply that opens with an operation heading (#623)
- agui: loop actions attach to a world and never mint one (#623)
- read: sibling channels are keyed by the fragment the model appends (#622)
- agui: the look action reaches the seam as a pattern read (#591)
- tools: a family summary stays within the catalog aside bound (#577)
- pattern: regex and glob run over the source text, not a projection (#591)
- mcp: a repeated list cursor fails the catalog, never a partial one (#601)
- providers: a usage refusal never fails the response (#580)
- providers: bounded cause evidence beneath the SDK's opaque wrapper (#593)
- demo: a specimen selector must match a registered story (#597)
- lifecycle: a completion defers to prompts the loop has not published (#610)
- feedback: word completion refusals at read time; retryable (#617)
- packet: child sections state emptiness; parent on the Worker block (#609)
- runtime: make TASK optional and align packet examples (#592)
- parser: preserve same-width nested fence bodies (#605)
- resources: bind runtime reads to their owning worker (#601)
- interactions: preserve identity and validate exact answers (#601)
- dogfood: preserve request failures and worker lifecycle (#602)
- resources: distinguish binary markers from stored bytes (#601)
- parser: accept annotations on unnamed SEND fences (#599)
- parser: treat unlabeled fences as literal SENDs (#598)
- feedback: name actual completion blockers (#596)
- core: space generated operations and keep visible programs complete (#592)
- core: name receipts after invoked executors (#595)
- contracts: align lean teaching and worker allocation (#594)
- teaching: share default worker capabilities with live specimens (#594)
- contracts: ignore interstitial text and preserve turn source (#590)
- core: close shutdown wake admission races (#587)
- core: align READ authority and fenced references (#582)
- docs: restore interpreter witnesses and align emission recovery (#582)
- parser: identify closed fences in boundary failures (#584)
- lifecycle: join cancellation settlement before shutdown (#583)
- lifecycle: preserve completion in mixed terminal inventories (#578)
- packet: clarify context curation wording (#578)
- lifecycle: allow final KILL without a completion strike (#578)
- lifecycle: observe every operation before completion (#578)
- parser: match complete backtick runs exactly (#578)
- gbnf: leave literal fence bodies opaque (#578)
- live: evaluate delivered SEND responses rather than raw packets (#578)
- packet: let policy documents own their headings (#578)
- lifecycle: preserve recovery allowance on unfinished joins (#581)
- drill: client conformance reaps stale preserved evidence at startup (#0)
- members: one owner reconciles membership after a definition change (#536)
- parser: the missing-SEND diagnostic states the lane rule (#574)
- exec: a tool run as a shell command is named at the failure site (#515)
- git: refuse automatic inspection when repo config declares a filter (#568)
- search: a member derivation failure is its failed row, not the turn (#536)
- engine: child terminal deltas are untargeted, attributed by source (#567)
- mimetypes: gate tree-sitter runtime init and grammar loads (#536)
- log: name the stream address when a channel misses an EXEC item (#502)
- git: pin fsmonitor and hooks off for automatic inspection (#568)
- runtime: guard worker policy writes against unfinished tasks (#573)
- runtime: preserve loop rails across waits and restart (#573)
- runtime: order causal admission and scope cancellation (#573)
- runtime: preserve execution allowance across waits (#573)
- runtime: publish truthful task and stream completion (#573)
- runtime: persist exact waits and preserve message ownership (#573)
- log: share curated source views across reads and transfers (#573)
- docs: keep authored invocation summaries on one line (#573)
- docs: keep interpreter summaries concise (#573)
- presentation: format JSON and complete tool summaries (#573)
- previews: bound automatic text acquisition through one selector (#572)
- question: collect answers through cancellable execution lifetimes (#571)
- worker: align transfers with read and mutation authority (#570)
- docs: pair shallow tool previews with complete input schemas (#566)
- parser: attribute terminal recovery at emission EOF (#524)
- mimetypes: preserve source identity on derivation failure (#532)
- worker: keep maintenance conclusions private (#565)
- allow verbatim plain text transfers (#560)
- packet: preserve scoped kill receipts (#563)
- runtime: execute authored operations in order (#562)
- edit: diagnose anchor collisions without assuming history (#561)
- test: propagate specimen cancellation and settle owned cleanup (#556)
- grammar: reserve structural headings without an operation quota (#551)
- packet: convert capacity into the stable curation ruler (#555)
- packet: foreground targets and effective tool invocations (#552)
- init: name the project filesystem in discovery (#550)
- loop: allow retrieval-only completion at the final strike (#549)
- reasoning: annotate automatic prior-turn observations (#548)
- packet: lead READ metadata with its target (#546)
- context: protect newly generated reasoning on overflow (#544)
- reasoning: stream leading envelopes before completion (#543)
- turns: settle one disposition after all operations (#540)
- recovery: treat missing channels as discovery misses (#536)
- rails: compare executed operations and results for cycles (#536)
- parser: keep trailing-text feedback factual (#534)
- parser: report actual delimiter and matcher boundaries (#534)

Performance:

- db: index owners, optimize at shutdown, dead columns (#614)
- db: index foreign-key check paths; plan witness over the registry (#614)

Refactors:

- db: graph referents, matched channels, stream cursors as sets (#624)
- core: a worker's obligations are one view over durable rows (#624)
- db: packet sections are rows over content-addressed items (#614)
- db: log pages, paste anchors, and glob lookups in one statement (#624)
- membership: reconciliation lands as two set statements (#624)
- log: a direct curation lands its whole plan in one statement (#624)
- fork: the branch's claimed row copies its history in a trigger (#624)
- db: the engine statements move beside their consumers (#624)
- lifecycle: cancellation is one bound statement and a trigger (#624)
- db: the schema baseline is eight domain chapters (#624)
- db: process triggers move beside their owners (#624)
- http: a page's source is its body; its markdown is #readable (#622)
- channels: the readable projection is the #readable channel (#622)
- init: the prompt row is its one publication; no turn-0 READ (#621)
- init: turn 0 archives nothing and names its spaces in asides (#621)
- gbnf: support an operator's grammar, generate none (#588)
- language: annotation and summary are the aside at every tier (#612)
- language: the metadata modifier moves from braces to brackets (#591)
- history: teach ordinary current-turn reasoning reads (#608)
- prompts: separate source identity from delivery order (#608)
- packet: retain native observations under log curation (#607)
- history: expose immutable turn sources through READ (#606)
- workspace: use explicit shared resource addresses (#604)
- workspace: share functionality and capability policy (#604)
- core: admit output without rewriting turns (#578)
- lifecycle: derive turn disposition from TASK inventory (#578)
- language: carry task inventory on NEXT and WAIT (#578)
- language: adopt executable fences and native dispositions (#578)
- lane: move every taught and tested lane from 0 to _ (#515)
- packet: retire Resources and repair reference maintenance (#576)
- skills: compose the Plurnk configuration tree (#569)
- exec: restore tool-owned invocation metadata (#569)
- skills: load native resource trees without flattening (#569)
- reasoning: use the prompt-style read-only source contract (#559)
- reasoning: use ordinary resources and budgeted READs (#545)
- search: replace embeddings with native FTS5 (#537)

Documentation:

- teaching: the delimiter rule rides its example as the aside (#615)
- changelog: the breaking and notable changes of the next publication (#615)
- contracts: the matcher rows describe the pattern option, not a body (#631)
- core: the packet markdown projection lives in the core spec (#626)
- schemes: the handler comment names LOOK alone (#634)
- teaching: the fence rule keeps its own callout (#621)
- teaching: tighten plurnk.md to the packet section names (#621)
- teaching: plurnk.md teaches the heading pattern option (#591)
- execs: the receipt body is the program as sent, never output (#594)
- teaching: worker.md describes KILL, no completion lecture (#619)
- teaching: operator draft: bracket metadata, final response SEND (#591)
- language: clarify the final-turn composition (#592)
- protocol: clarify operation and curation teaching (#594)
- contracts: streamline task and curation guidance (#601)
- exec: use invocation terminology for input ownership (#600)
- teaching: clarify enclosing fence lengths without adding a rule (#603)
- teaching: name the SEND reply example (#603)
- teaching: clarify SEND reply orientation (#603)
- teaching: foreground workflow and clear default policy (#592)
- contracts: keep BARE resource inputs in deep docs (#594)
- language: demonstrate annotations in every operation signature (#592)
- language: clarify task inventories and shared annotations (#592)
- grammar: clarify completion and log curation (#578)
- grammar: teach longer outer fences for nesting (#584)
- teaching: clarify curation with Markdown admonitions (#578)
- providers: qualify constrained decoding tradeoffs (#578)
- teaching: reflect pending work in the example inventory (#578)
- teaching: demonstrate nested fences in SEND (#578)
- teaching: keep the concise final-turn rule (#578)
- language: preserve the approved TASK teaching (#578)
- teaching: adopt policy workflow and retain ACP curation steering (#578)
- contracts: align parser and steering references with fences (#578)
- manual: the lane is literal on every op; anchors are the way to edit (#0)
- manual: a body is literal and unfenced (#0)
- manual: the hot path shows a body-only bare (#577)
- reference: family doc-file seam, authored bodies, header-only marks (#577)
- reference: show unfenced multi-line bodies in the deep docs (#577)
- mimetypes: cite the runtime-gate issue in local shorthand (#536)
- runtime: align timeout and polling units (#573)
- reconcile subsystem references with current contracts (#553)
- refine log curation guidance (#549)
- orient stream reads around their source (#553)
- clarify log-local reasoning curation (#549)
- jq: correct executor selection in materialized examples (#553)
- prompt: recommend retiring stale or superseded content (#0)

Reverts:

- pattern: restore regex and glob over the readable projection (#591)

## 1.16.5 — 2026-09-05

Fixes:

- contracts: localize invalid scope diagnostics (#531)
- mimetypes: normalize parser CRLF boundaries (#530)

## 1.16.4 — 2026-09-04

Fixes:

- reports: measure current packets and worker topology (#515)
- installation: ship every supported source grammar (#521)
- release: reject unsuccessful daemon teardown (#520)
- mimetypes: separate acquisition and teardown failures (#519)

## 1.16.3 — 2026-09-04

Fixes:

- digest: preserve native attachment selection per request (#518)
- orientation: separate memory guidance and exercise overflow recovery (#515)
- transfers: resolve new destination scopes against empty content (#514)
- log: preserve overflow history and turn programs (#513)
- multimodal: deliver READ attachments exactly once (#493)

Documentation:

- teaching: group syntax orientation beside its example (#515)

## 1.16.2 — 2026-09-04

Fixes:

- mimetypes: align grammar runtime peers (#512)
- release: publish managed leaves before consumers (#512)
- multimodal: compose ranged binary attachments (#493)

## 1.16.1 — 2026-09-04

Fixes:

- providers: restore Fireworks replica affinity (#510)

## 1.16.0 — 2026-09-04

Features:

- core: a binary lives in a worker entry, stored and read as bytes (#504)
- core: binary splices region-into-region on COPY and MOVE (#504)
- core: binary files copy and move, whole or by byte range (#504)

Fixes:

- release: registry visibility waits out npm's replication lag (#650)
- release: the audit gate recognizes npm's actual timeout text (#649)
- release: the audit gate is bounded and advisory when unreachable (#649)
- scripts: journey model and gbnf corpus speak the landed grammar (#509)
- core: drop the unused worker id the tag removal left behind (#648)
- providers: the operation deadline is race-enforced, not advisory (#505)
- core: a binary entry is searched as bytes, never its base64 text (#504)
- contracts: an exec executor may trail the path, not only lead (#503)

Refactors:

- packet: tight headers, path-pointer sections as JSON arrays (#509)
- core: delete the dead log_tags classification subsystem (#648)
- core: the budget opens as one JSON object, like ## PLAN0 (#509)
- contracts: one hierarchy - # title, ## block, ### op (#509)
- packet: frame log entries with Markdown (#509)

Documentation:

- the release train in the operations quick reference (#649)
- contracts: a bare KILL deletes an entry, a scope folds a span (#507)

## 1.15.0 — 2026-09-03

Features:

- core: git.md skill surfaces for git-backed workspaces (#468)
- attachments ride only where the route accepts them (#497)
- core: turn zero names memory items (#484)
- example is the operation-example fence language (#484)
- core: turn zero says determinations persist as memories (#484)
- core: an image member rides the packet as a native part (#493)
- mimetypes: image plugin: png, jpeg, gif, webp read as header facts (#493)
- core: binary resources read and find as bytes (#493)
- core: an exec with <-1> outlives its loop (#494)
- core: deletion receipts quote the removed text (#489)
- schemes-http: kill cancels or forgets; {remote} deletes (#489)
- core: semantic find rows carry their cosine similarity (#489)
- contracts: exec takes an executor slot; the path is the program (#489)
- core: eject FOLD/OPEN and the signal slot; labels, KILL scope (#489)
- agui: synchronize durable conversations (#490)
- core: calibrate the curation readout to the answering model (#487)

Fixes:

- core: survey summaries keep the witness, drop the code span (#484)
- candidate: reap daemon after client signal (#498)
- core: a cycle fingerprint digests the whole op body (#495)
- contracts: the matcher placement diagnostic shows the form (#489)
- core: a scoped entry kill with a body pattern is refused (#489)
- core: a non-member on disk is refused, never absent (#489)
- core: a failed command's stream reads as its content (#489)
- scripts: client journey spells the bare shell (#489)
- core: exec streams live at the item address /loop/turn/item/EXEC (#489)
- agui: scope threads by workspace (#491)
- agui: preserve replay and interrupt continuity (#488)
- scripts: name the sibling client checkout conformance needs (#484)
- schemes-http: declare the plurnk-meta dependency it imports (#484)

Refactors:

- core: exec path is a registered runtime or the shell target (#489)
- core: extract TurnRunner's admitted-turn execution (#484)
- agui: extract the Run endpoint handler (#484)
- core: extract git membership's materialization (#484)
- core: extract Engine's loop driver (#484)
- agui: extract the built-in actions (#484)
- schemes-http: extract the GET and request paths (#484)
- core: extract Daemon's client reads (#484)
- providers: extract the AI SDK request-body builders (#484)
- core: extract Daemon's worker-model resolution (#484)
- core: extract TurnRunner's materialization and bare batch (#484)
- core: extract Dispatcher's statement handlers (#484)
- core: split ResourceMutations into edits, selection, transfers (#484)
- core: split Digest into reader, renderers, requiem, rows (#484)
- drop exports and re-exports nothing imports (#484)

Documentation:

- contracts: kill is a may (#484)
- contracts: fenced examples are labelled example (#484)
- contracts: kill teaching lists one form per target (#484)
- meta: consolidate verification policy
- plurnk.md ties the kill mandate to tokensActiveTotal (#489)
- plurnk.md drops the range override, anchors a file kill (#489)
- scheme documents speak the landed grammar (#489)
- plurnk.md says proceed, not perform ops (#489)
- agents: operations quick reference for every agent (#484)
- plurnk.md teaches KILL whole, drops the slot rule, says path (#489)
- plurnk.md fixes the log KILL example and the path escape list (#489)

## 1.14.2 — 2026-09-01

Features:

- core: contract strikes - one mandate, one rail, every violation (#479)
- candidate photographs the project root at the grade deadline (#478)
- providers: exact slack flexes the response allowance (#482)
- sampling passes through to the provider; strikes return to 3 (#480)
- providers: retries narrow to provider-directed waits (#479)
- core: length cuts name the allowance; packets carry responseMax (#478)

Fixes:

- core: tokensResponseMax discloses the program room, not the total (#478)
- providers: llama-server rails admit the stated reasoning effort (#483)
- providers: the template style forwards the stated reasoning effort (#483)
- providers: output conformance judges the grant, not the floor (#482)
- providers: overflow is tolerated, never invited (#482)
- core: truncation notices state the fact and drop the steering (#478)
- providers: an effort refusal names the operator declaration lever (#472)
- core: a cut too deep to parse names the truncation, not the parser (#478)
- core: fresh schemas defer the reasoning vocabulary to contracts (#474)

Refactors:

- core: one derivation decides a ceiling cut on every path (#478)

Documentation:

- model text drops the EDIT exactness threat and the EXEC permission (#478)
- providers: rails on weak models need the frame command in the recap (#477)

## 1.14.1 — 2026-08-31

Features:

- contracts: max and xhigh join the portable reasoning vocabulary (#474)
- digest: render cost kind and wire error line (#473)

## 1.14.0 — 2026-08-31

Features:

- core: branch delegation meets the specification bar (#396)
- core: turn_generated carries the settled wire accounting (#465)
- models: provider segments are models.dev ids; old names refuse (#459)
- providers: operator cost override merges over models.dev rates (#461)
- core: model routes carry the durable reasoning policy (#467)

Fixes:

- embeddings: race a wire deadline on every physical request (#463)
- core: digest health treats executor completion rows as evidence (#436)
- core: contain per-candidate content-handler crashes in FIND (#449)
- mimetypes: survive readability crashes on template partials (#449)
- providers: ship reasoning styles under models.dev id prefixes (#458)
- providers: adaptive declares the toggle on for reasoning routes (#457)
- core: name usage-less settled requests on the digest cost line (#461)
- core: budget requiem evidence to the witness window (#448)
- providers: thread reasoning budget to the openrouter wire (#466)
- contracts: scope error names anchor provenance (#452)
- providers: 2xx invalid-response retries within the bounded budget (#446)

Refactors:

- agui: drop the branch-batch event route (#396)
- core: delete branch delegation outright (#396)

Documentation:

- meta: delegation modal shaves to should (#462)
- meta: verification discipline in policy (#447)

Reverts:

- contracts: drop the anchor-provenance parenthetical (#452)

## 1.13.0 — 2026-08-30

First tagged release.
