# Optional peers stay phantom. The native tree-sitter binding is never imported
# (web-tree-sitter is the runtime; grammar devDeps are wasm/compile fixtures), its
# peerOptional ranges conflict across grammars, and its node-gyp build fails —
# the pre-monorepo lockfiles never materialized it either.
omit=peer
