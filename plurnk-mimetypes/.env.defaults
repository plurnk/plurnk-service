# @plurnk/plurnk-mimetypes — binary input admission.

# Maximum source bytes per binary-handler invocation, positive integer.
# Rejected before retaining the full source; format-specific limits may be stricter.
PLURNK_MIMETYPES_BINARY_INPUT_MAX_BYTES=104857600
