# --- @plurnk/plurnk-execs-isogit : optional in-process Git subset ---
# Native EXEC[git] is the default. Enable this distinct sandbox-oriented
# capability explicitly; it never replaces or falls back from native Git.
PLURNK_EXECS_ISOGIT=0
