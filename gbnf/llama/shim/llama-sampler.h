#pragma once
// llama-grammar.cpp #includes this but references no sampler symbols on the
// validator path, so the shim is intentionally empty. Hand-written adaptation —
// NOT a verbatim upstream copy (see llama/PROVENANCE.md).
