{
    "plurnk-a2a/src/PlurnkTaskStore.ts:UNSPECIFIED_PAGE": "the A2A protocol's own page size for a ListTasks request that names none",
    "plurnk-agui/src/config.ts:MAX_TIMER_MS": "the platform's setTimeout ceiling (2^31-1 ms); a bound on what a heartbeat knob may say",
    "plurnk-agui/src/observe.ts:MAX_STRING_LENGTH": "telemetry attribute truncation: bounds a span attribute, changes no behaviour",
    "plurnk-core/src/core/attachments.ts:AUDIO_TOKENS_PER_SECOND": "a stable curation unit by design: persisted weights depend on it, and calibration adjusts capacity instead",
    "plurnk-core/src/core/attachments.ts:PDF_BYTES_PER_TOKEN": "a stable curation unit by design: persisted weights depend on it, and calibration adjusts capacity instead",
    "plurnk-core/src/core/attachments.ts:PDF_TOKENS_PER_PAGE": "a stable curation unit by design: persisted weights depend on it, and calibration adjusts capacity instead",
    "plurnk-core/src/core/BudgetReadout.ts:MAX_WIDTH_PASSES": "iteration bound of a fixed-point width computation",
    "plurnk-core/src/core/file-materialization.ts:STORAGE_MAXIMUM_BYTES": "the schema's CHECK on a channel body; the bound on what PLURNK_SERVICE_FILE_MATERIALIZE_MAX_BYTES may say",
    "plurnk-core/src/core/LoopDriver.ts:HOLD_POLL_MS": "how often a held turn re-checks its spawns; the hold's length is PLURNK_SERVICE_EXEC_HOLD_MS",
    "plurnk-core/src/core/TokenCalibration.ts:MIN_SAMPLES": "statistical floor of the calibration estimator; changing it is changing the algorithm",
    "plurnk-core/src/observe/spans.ts:MAX_STRING_LENGTH": "telemetry attribute truncation: bounds a span attribute, changes no behaviour",
    "plurnk-core/src/server/SkillsFunctionality.ts:CLI_OUTPUT_BYTES": "child-process output buffer ceiling; the run's deadline is PLURNK_SERVICE_SKILLS_CLI_TIMEOUT_MS",
    "plurnk-core/src/server/WorkspaceCapabilities.ts:COOLING_RETRY_MS": "internal retry cadence of a deferred cool-down; residency itself is PLURNK_SERVICE_WORKSPACE_WARM_MS",
    "plurnk-mcp/src/inputRequired.ts:INPUT_REQUIRED_MAX_ROUNDS": "the adapter's statement of the MCP SDK's multi-round bound; protocol resilience, not a deployment choice",
    "plurnk-mcp/src/inputRequired.ts:REQUEST_STATE_ONLY_PACING_MS": "protocol pacing between state-only rounds",
    "plurnk-mcp/src/mcp-watchdog.mjs:PARENT_POLL_MS": "parent-death detection cadence of the stdio watchdog",
    "plurnk-mcp/src/mcp-watchdog.mjs:SHUTDOWN_GRACE_MS": "the stdio watchdog's graceful-exit slack before it kills its server",
    "plurnk-mcp/src/subscriptions.ts:RETRY_CEILING_MS": "the adapter's one retry pacing; the deadlines an operator tunes are PLURNK_MCP_CONNECT_TIMEOUT and PLURNK_MCP_REQUEST_TIMEOUT",
    "plurnk-mcp/src/subscriptions.ts:RETRY_FLOOR_MS": "the adapter's one retry pacing; the deadlines an operator tunes are PLURNK_MCP_CONNECT_TIMEOUT and PLURNK_MCP_REQUEST_TIMEOUT",
    "plurnk-mcp/src/tasks.ts:MINIMUM_POLL_INTERVAL_MS": "the least poll interval the adapter will honour from a Task server",
    "plurnk-mcp/src/tasks.ts:OWN_POLL_INTERVAL_MS": "the adapter's cadence when a Task server suggests none; the server's suggestion is the real control",
    "plurnk-mimetypes-application-pdf/src/ApplicationPdf.ts:SCAN_LIMIT": "bound on a page-tree scan that keeps a pathological file cheap; PLURNK_MIMETYPES_BINARY_INPUT_MAX_BYTES admits the file",
    "plurnk-mimetypes/src/TextCoordinates.ts:LINE_PREVIEW_CODEPOINTS": "length of a line excerpt inside a range error message",
    "plurnk-providers/src/AiSdkProvider.ts:OPERATION_DEADLINE_UNWIND_GRACE_MS": "unwind slack after a deadline already fired; the deadlines themselves are provider knobs",
    "plurnk-providers/src/AiSdkRequestBody.ts:MANUAL_REASONING_MINIMUM": "the provider protocol's own minimum reasoning allowance",
    "plurnk-providers/src/capacity.ts:WIRE_FLEX_MARGIN": "safety margin of the flexed-allowance arithmetic; the budgets it protects are provider knobs",
    "plurnk-providers/src/env.ts:MAX_PROVIDER_TIMEOUT_MS": "the platform's setTimeout ceiling (2^31-1 ms); a bound on what a timeout knob may say",
    "plurnk-schedule/src/Functionality.ts:PREVIEW": "upcoming occurrences shown in a rule listing",
    "plurnk-schedule/src/Scheduler.ts:DIAGNOSTIC_LIMIT": "length of a diagnostic excerpt inside a delivery Problem",
    "plurnk-schedule/src/Scheduler.ts:MAX_DELAY_MS": "the platform's setTimeout ceiling (2^31-1 ms); a farther occurrence arms in hops",
    "plurnk-schemes-http/src/Http.ts:DELTA_SECONDS_LIMIT": "RFC 9111's delta-seconds ceiling (2^31)",
    "plurnk-schemes-http/src/HttpGet.ts:LLMS_TEXT_ATTEMPT_TTL_MS": "negative cache of an origin's llms.txt probe; courtesy pacing, not acquisition policy"
}
