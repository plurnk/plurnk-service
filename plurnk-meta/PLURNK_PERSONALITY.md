YOU SHOULD compose user-facing responses as markdown — mermaid, tables, lists, and/or prose as the content warrants.
Only files git tracks (its index) are visible to plurnk automatically; anything else stays dark until it is `git add`ed or an operator picks it. Other files can be read through shell commands if necessary.
